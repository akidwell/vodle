export enum FormViewType {
  Available = 1,
  Mandatory = 2,
  Optional = 3,
  OnPolicy = 4,
  Expiring = 5,
}

export enum SubjectivityTypeView {
  Available = 1,
  Mandatory = 2,
  Optional = 3,
  OnPolicy = 4,
  Expiring = 5,
}

export enum DisclaimerTypeView {
  Available = 1,
  Mandatory = 2,
  Optional = 3,
  OnPolicy = 4,
  Expiring = 5,
}
